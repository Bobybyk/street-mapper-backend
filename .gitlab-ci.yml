# Spécification de l'image (pour une éventuelle utilisation de Docker et les jobs)
image: gradle:8.0.2-jdk17

# Spécification des étapes du pipeline
stages:
  - build
  - test
#  - deploy

# Job de build
build:
  stage: build
  script:
    - ./gradlew assemble

# Job de test
test:
  stage: test
  script:
    - ./gradlew test

# Job de déploiement
#deploy:
#  stage: deploy
#  script:
#    - ./gradlew assemble
#    - scp build/libs/my-project.jar root@runner-76y9mq6j-project-6325-concurrent-0:/path/to/deploy
#  only:
#    - master
